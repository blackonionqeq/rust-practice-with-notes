# 02 Try Shadowing

## Goal

Practice ownership transfer and return, tuple destructuring, and shadowing.

## Required Function

```rust
fn measure(text: String) -> (String, usize)
```

## Review Result

Passed.

The only issue found was formatting around `->` and tuple commas, fixable with `cargo fmt`.
